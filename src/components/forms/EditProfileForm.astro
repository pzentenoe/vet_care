---
import EditProfileFormSolid from '@components/solid/EditProfileForm';
---

<div id="edit-profile-form-container" data-profile-form>
    <EditProfileFormSolid client:visible isOpen={false} onClose={() => {}} />
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const formContainer = document.getElementById('edit-profile-form-container');

        // Definir función global para abrir el modal
        window.openEditProfileForm = () => {
            // Disparar evento custom que el componente Solid escuchará
            const event = new CustomEvent('open-profile-form');
            formContainer?.dispatchEvent(event);
        };

        // Exponer función al objeto window
        if (typeof window !== 'undefined') {
            window.openEditProfileForm = window.openEditProfileForm || (() => {});
        }
    });

    // Declaración para TypeScript
    declare global {
        interface Window {
            openEditProfileForm: () => void;
        }
    }
</script>